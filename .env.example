# HealthOps UI Test Automation — environment variables
# Copy to .env and adjust values for your target environment.

# Target environment: local | staging | production
HEALTHOPS_ENV=local

# Application base URL (overrides environment defaults)
HEALTHOPS_BASE_URL=http://localhost:3000

# Browser execution
HEADLESS=true

# CI flag (set automatically in pipelines)
# CI=true
