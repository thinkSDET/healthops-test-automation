/**
 * Shared TypeScript types for HealthOps UI automation.
 * Placeholder — domain types to be added as implementation progresses.
 */

export type HealthOpsEnvironment = 'local' | 'staging' | 'production';
